@page "/logout"

@inject IAuthenticationRepository _authoRepo
@inject NavigationManager _navMan

@code {
    protected override async Task OnInitializedAsync()
    {
        await _authoRepo.Logout();
        _navMan.NavigateTo("/");
    }
}
